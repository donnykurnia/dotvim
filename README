Installation:

	git clone git://github.com/donnykurnia/dotvim.git ~/.vim

Update submodules:

  cd ~/.vim
  git submodule init
  git submodule update

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/gvimrc ~/.gvimrc

